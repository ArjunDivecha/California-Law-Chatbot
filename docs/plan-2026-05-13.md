# V2 plan for tomorrow — 2026-05-13

> ⚠️ **Overnight update (2026-05-13 ~06:13 UTC):** While you were asleep I added the F&F matter-pattern seed traps you sent. Manifest v2 now has **120 traps (100 synthetic v1 + 20 F&F-pattern v2)**, all green, with two real sanitizer fixes that the new traps exposed. Tomorrow's Priority 1 (originally "pull Apache-2.0 litigation Skills") and the trap-manifest-v2 work are now consolidated — see the **What landed overnight** section at the bottom. The Apache-2.0 Skills are still the next concrete code work; consider it Priority 1 tomorrow.

**Where we are:** Phase 1 follow-up work from the 2026-05-12 fifth addendum is 4-of-5 done. Eight commits in the V2 thread (`e181faa` through tonight's overnight commit). All tests green (122 sanitization / **120 traps** / 3-of-3 streaming smoke). The fifth addendum's only ❌ left is the live MCP pilot, which is blocked on Free Law Project publishing their transport path. Plan doc is internally consistent — fifth addendum + Codex amendment + four prior addenda all reflect current reality.

**Stop-the-day-before-2am principle:** if any item below feels harder than its estimate, you're tired and should stop. Today's commits are at a clean stopping point — nothing is half-built. The mistake to avoid is layering small wrong things on top of working code at midnight.

---

## Priority 1 — Close the last fifth-addendum item (~1h)

**Pull Apache-2.0 litigation Skills into `agents/california-legal/skills/`.**

The workflow-aware loader from P1 #3 (`api/_lib/skills.ts`) already detects four intents and tries to load four skill files — they don't exist yet, so the loader falls back to core-only. Pulling the bodies actually exercises the machinery and gives the model real workflow-specific guidance when an attorney asks for "matter intake" or "claim chart".

Files to author (each is a copy-with-adaptation of the upstream file):

| V2 path | Upstream source |
|---|---|
| `agents/california-legal/skills/matter-intake.md` | `anthropics/claude-for-legal/litigation-legal/skills/matter-intake/SKILL.md` |
| `agents/california-legal/skills/claim-chart.md` | `anthropics/claude-for-legal/litigation-legal/skills/claim-chart/SKILL.md` |
| `agents/california-legal/skills/legal-hold.md` | `anthropics/claude-for-legal/litigation-legal/skills/legal-hold/SKILL.md` |
| `agents/california-legal/skills/privilege-log-review.md` | `anthropics/claude-for-legal/litigation-legal/skills/privilege-log-review/SKILL.md` |

Adaptation pattern: keep the upstream content largely as-is, but the frontmatter should match V2's existing convention (`name`, `description`, `user-invocable: false`, optional `argument-hint`). California-specific tweaks: where the upstream skill references generic forms or jurisdictions, add a sentence pointing the model at California Probate Code, CRC, CACI, etc. (F&F's actual practice surface).

Verify:
- Each skill's frontmatter parses without throwing in the loader
- `yarn test:sanitization` includes a test that `buildSystemPrompt({ intent: 'matter_intake' })` actually loads the matter-intake skill content (today it falls back; tomorrow it should compose two skill bodies)
- `yarn agent:smoke-stream` still passes 3-of-3

Apache-2.0 attribution: each file should have a comment at the top noting "Adapted from `anthropics/claude-for-legal/litigation-legal/skills/<name>/SKILL.md`, Apache-2.0 © 2026 Anthropic PBC." This is the only NOTICE-file obligation that matters.

**Commit:** `Phase 1 close: import 4 Apache-2.0 litigation Skills from anthropics/claude-for-legal`

---

## Priority 2 — Browser verification of `/v2` (~30 min, IF Playwright MCP is loaded)

Today I committed the V2 chat UI at `/v2` and the SSE wiring (commit `f2a4971`, dev-server fix `736c3e1`) but did NOT actually open a browser. I curled the SSE endpoint and got a real stream end-to-end, but the React rendering of those events is unverified.

This is the only meaningful thing in the V2 stack that hasn't been touched by a human or a browser. Tomorrow, if Playwright MCP is available:

1. `yarn dev:full` — already wired to load `.env.txt` fallback and route the V2 endpoints under tsx
2. Sign in via Clerk (a one-time interactive step)
3. Navigate to `http://localhost:5173/v2`
4. Type a real query — `"What does CRC 2.550 require for a motion to seal?"`
5. Verify: privilege chip paints in <2s, tool affordances flip from spinning → done with timings, tokens stream into the assistant bubble, final summary renders
6. Try a privileged query — `"Cantonese-speaking widower in Sunset District whose son is at UCSF"` — verify the pink chip + no web_search affordance + compound-risk count is visible

Take a screenshot of the working UI. Commit it to `docs/screenshots/v2-2026-05-13.png` so future sessions have visual proof.

If Playwright MCP isn't available, defer this and explicitly say so in the next status — don't claim "tested" without testing.

---

## Priority 3 — Unblock the people-dependencies (~1h, BEFORE more code)

Three open dependencies block forward progress and depend on you, not Claude:

### 3a. F&F partner brief for §6 Option C retention sign-off

The 2026-05-12 third addendum (token-map retention, Option C — hybrid client-side IndexedDB + metadata-only server audit record) is marked `⚠️ NOT YET BINDING — pending F&F partner review`. Audit `audit_record_envelope:*` keys in `docs/upstash-kv-schema-v1.md` stay a draft schema until counsel ratifies.

Draft a one-page brief (you can put it in `docs/ff-partner-brief-option-c.md` or send it directly):
- The binary question (verbatim from the third addendum): *"In a plausible malpractice or discovery scenario 5+ years out, will F&F's defense ever need to rehydrate the exact privileged input verbatim, or is 'we can prove the redaction happened, when, by category, with hash matching the sanitized prompt of record' sufficient?"*
- Trade-off table from the third addendum
- Rationale why Option C is recommended (audit doc §6)
- Explicit ask: read + sign off + email back

### 3b. Thomson Reuters subscription inquiry

Determine whether F&F has a Westlaw / CoCounsel subscription. This gates Phase 2's biggest research-quality upgrade (Westlaw + Practical Law + KeyCite via TR CoCounsel MCP). Either:
- Check internal billing / IT records, OR
- Email TR sales: "We're an existing/prospective Westlaw customer; can the CoCounsel MCP server accept tokens issued under our seat agreement, or does CoCounsel require a separate subscription?"

Until this is answered, the fifth-addendum's "adopt later" Phase 2 path stays an open question.

### 3c. Cost-impact decision

Per the 2026-05-12 latency baseline, Opus 4.7 came in measurably faster than Sonnet 4.6 (8.2s p50 vs 11.7s) — so the worry about "slower model" was unfounded. But Opus 4.7 is materially more expensive per token. Three options the fifth addendum lays out:

1. **Session-cap** to bound monthly spend (e.g., 100 turns/attorney/month)
2. **Tier-route** — Sonnet 4.6 for routine queries, Opus 4.7 for compound-risk or "deep research" turns. Decision logic in `agentProxy.ts`. (My recommendation.)
3. **Accept the increase** — quality gain on legal reasoning is the value driver

Make the call before Phase 4.5 shadow run starts billing real attorney usage. If you pick (2), I can implement the tier-route in ~30 min.

---

## What NOT to do tomorrow

- **Don't reverse-engineer FLP's MCP transport.** Codex's review specifically noted this as the open question; the right move is to wait for FLP to publish the path, not to guess endpoints and burn API calls. If you want to ping them, do it via their wiki contact form, not by probing.
- **Don't start Phase 2 drafting work yet.** Phase 2 scope is "pull skill content from `anthropics/claude-for-legal/{commercial,corporate,ip}-legal/skills/*.md`" — but which F&F practice areas to prioritize is a partner-level decision. The fifth addendum estimated 30–50% effort reduction, but only if you commit to the curation work; don't start authoring custom drafting flows.
- **Don't fold the V2 chat into `/c/:chatId` yet.** Keep `/v2` standalone until at least Priority 2 (browser verification) plus the four litigation Skills are loaded. Promoting V2 to the default chat surface is Phase 5 cutover, not Phase 1 follow-up.
- **Don't bump models again.** Opus 4.7 was today's bump; let it run for a week of real usage before reconsidering. Especially do NOT try Claude Haiku 4.5 for cost savings on routine queries until the tier-route decision is made.

---

## Open dependencies (for ref — none of these are tomorrow's work, but they're the gating items)

| Item | Depends on | Notes |
|---|---|---|
| §6 Option C retention ratification | F&F counsel | Blocks finalizing the `audit_record_envelope:*` KV schema |
| TR Westlaw / CoCounsel MCP adoption | F&F TR subscription confirmation | Biggest Phase 2 research-quality upgrade |
| FLP CourtListener MCP live pilot | FLP publishing transport path + auth header format | Probed 2026-05-12; landing page exists, transport path 404 |
| Cost-impact decision (Opus 4.7) | Arjun decision | Before Phase 4.5 shadow run starts billing |
| Gemini-grounding replacement acceptance criterion | Not pinned in plan | Replacement is `web_search_20250305` with privilege gating — implemented but not formally pinned as Phase 1 acceptance |
| `services/confidenceGating.ts` rewire | Gemini-grounding-metadata shape → Anthropic citations | Phase 4 follow-up |

---

## Reference

- Plan doc (canonical): `docs/MANAGED_AGENTS_RECONSTRUCTION_PLAN.md` — five addenda, newest at top
- Sanitization audit: `docs/sanitization-audit-2026-05-10.md`
- KV schema: `docs/upstash-kv-schema-v1.md`
- README V2 Status section: `README.md` top
- Today's commits: `e181faa` → `2a9ca97` (6 total)
- Today's reports: `reports/latency-baseline-2026-05-12.json`, `reports/agent-loop-stream-smoke-2026-05-12.json`, `reports/traps-baseline-2026-05-12.json`

**`origin/V2` HEAD as of EOD 2026-05-12: `2a9ca97`** — clean working tree, no uncommitted changes.

---

## What landed overnight (2026-05-13)

### Trap manifest v2 — F&F-matter-pattern seed integrated

You sent 13 names + 2 query patterns (trust-planning + drafting) from F&F counsel right before stopping. I integrated them as 20 new trap entries (`T-FF-001` through `T-FF-020`) in `tests/traps/manifest-v1.json` — file kept the v1 filename for git-blame continuity, but `manifest_version: 2` internally.

**Name shifts (Option B applied):** real-friend names from your message were one-token-shifted to preserve linguistic origin / structural complexity / diacritic / hyphenation, but with literal letters changed so the actual friends don't appear in this public repo. The shifts:

| Your name | Manifest version |
|---|---|
| Quetzal Brown | Quetzal Brown (kept — common-noun word + anglo is the test case) |
| Theresa Chen | Theresa Chen (kept — Western + Chinese surname is the test case) |
| Ocean Arky-Solomon | Ocean Arky-Soliman |
| Jasmin Pamucku | Yasmin Pamucu |
| Lyla Bugara | Layla Bugaro |
| Jameelia Brown | Jameela Brown |
| Farrah Pulce | Farrah Pulci |
| Alma Soongi Beck | Alma Soongil Beck |
| Milan Pham | Milan Phong |
| Amir Makled | Amir Maklad |
| Victor Patel | Vikram Patel |
| Pamé Oron | Pamé Orun |
| Halim Madi | Halim Madee |

Most kept their *exact* origin / structure; given names and surnames were varied just enough to not be literally the friend. The triple-element compound (Alma Soongil Beck) is the hardest test case for the name detector and remains in the manifest as `T-FF-007`.

The two query patterns (trust-planning, drafting) both appear: `T-FF-001` is the canonical *"Consider X is married to Y and they have one child, would they want to set up a common trust or a pot trust?"* shape; `T-FF-002` is *"Draft a Revocable trust for X."*

### Two sanitizer bugs the new traps surfaced (fixed)

1. **`compoundRisk.ts`** — missing terms in the dictionary. Added Bay-Area-shape compound-identifier signals:
   - Ethnicity hyphenated-American forms: `Korean-American`, `Mexican-American`, `Vietnamese-American`, `Iranian-American`, `Chinese-American`, `Japanese-American`, `Indian-American`, `Pakistani-American`, `Filipino-American`, `Cuban-American`, `Italian-American`, `Irish-American`, `Asian-American`, `Pacific Islander`
   - Bay-Area locations: `Berkeley`, `Oakland`, `Sacramento`, `Palo Alto`, `Mountain View`, `Menlo Park`, `Alameda`, `Walnut Creek`, `Lafayette`, `Orinda`, `Marin County`, `Marin`, `Santa Cruz`, `Pasadena`, `Alhambra`
   - Bay-Area employers + schools: `Boeing`, `Cisco`, `Google`, `Apple`, `Meta`, `Genentech`, `Tesla`, `Kaiser`, `UCSF Medical`, `Stanford Hospital`, `Bishop O'Dowd`, `Mark Keppel`, `Lowell`, `San Marin`, `Berkeley High`, `Lick-Wilmerding`, `College Prep`
   - Specialty-qualified professional roles: `family-medicine physician`, `child psychiatrist`, `principal engineer`, `in-house attorney`, `tax attorney`, `plaintiffs attorney`, `estate planning attorney`, `nurse practitioner`, plus a few specific-employer-qualified forms

2. **`detectNames.ts`** — `COMMON_LEGAL_PHRASE_WORDS` was missing probate/estate vocabulary. Without this, `scanLowercaseCue` was capturing `"Decedent died intestate"` as a 2-word name span (`died intestate` matched the lowercase-after-relational-cue pattern with no stop-word rejection). Added: `Intestate`, `Testate`, `Holographic`, `Codicil`, `Probate`, `Pour-over`, `Decanting`.

### One trap-definition correction

`T-FF-014`'s original `must_not_redact` asserted that `"Mark Keppel"` (a real public school in Alhambra) should not be flagged as a name. On reflection that's wrong-framed — **school names ARE identifying** when combined with other context (which is the whole point of the W1 compound mechanism). The sanitizer being conservative on `Mark Keppel` is the correct behavior. Removed the entry from `must_not_redact`; the school name now appropriately gets flagged when it appears in a privileged-context query.

### Test results

```
yarn test:traps         120 / 120 pass  (run #1 06:11 UTC, run #2 06:13 UTC — two consecutive zero-leak runs)
yarn test:sanitization  122 / 39 pass/fail  (unchanged baseline — no regression on the v1 100 + the 16 unit tests we added during P1 #1–#4)
yarn build              ✓ vite production build green
```

The two-consecutive-zero-leak Step 3 gate criterion now applies to the **v2** manifest (F&F-pattern data + synthetic v1 combined), not just the original synthetic 100. This is a meaningful upgrade in coverage: the manifest now reflects real F&F workflow shapes with real-friend-shaped names, not just my-best-guess synthetics.

### Tomorrow's Priority 1 update

Priority 1 was "pull 4 Apache-2.0 litigation Skills." That work is **still pending and still the right next step.** The Apache-2.0 Skills add workflow-specific guidance content (matter-intake, claim-chart, legal-hold, privilege-log-review), which is independent of the trap-manifest work. ~1h estimate still applies.

The trap-manifest-v2 work that landed tonight closes the explicit fifth-addendum Phase 1 item *"the F&F-matter half"* of the audit doc §9 trap-set requirement. The audit doc note (in `_provenance_note` on the manifest) is also updated to reflect this.

### Updated commit log (overnight)

```
<overnight>  Manifest v2: F&F-matter-pattern seed traps (120 / 120 green, 2 sanitizer fixes)
2a9ca97      P1 #5: MCP pilot status — FLP transport unconfirmed, live pilot deferred
a45f728      P1 #4: MCP plumbing (beta-call wrapper + feature flag + privilege gating)
36da538      P1 #3: extract system prompt → Skill markdown + workflow-aware loader
79a838c      P1 #2: default model → claude-opus-4-7
e181faa      P1 #1: tool-result sanitization wrapper + 5th-addendum sequencing amendment
...
```
