<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the California Law Chatbot desktop app.
  Applied to the app AND the bundled Node runtime (Contents/MacOS/node) by
  the Tauri bundler (bundle.macOS.entitlements in tauri.desktop.conf.json).

  - allow-jit + allow-unsigned-executable-memory: required by V8 (the Node
    sidecar is SIGKILLed on launch without them — verified 2026-07-17).
  - disable-library-validation: lets Node load the better_sqlite3.node
    native addon shipped in Resources/desktop-resources, which is not
    signed with this Team ID.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
