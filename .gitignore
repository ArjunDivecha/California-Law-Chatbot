# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# CEB Processed Data (large files, not tracked)
data/
scripts/data/

# Python cache
__pycache__/
*.pyc
*.pyo
.local-secrets/
.envrc
.env*

# Installer build artifacts (multi-GB: venv, GLiNER weights, hf-cache, .pkg)
installer-pkg/build/
installer-pkg/payload/

# Editor / tooling local state
.cursor/
.yarn/install-state.gz

# Local-only preserved artifacts (secrets / personal files — never commit)
archive-env-2026-07-01/
Etf pair trading -*
Finrobot-*
Proposal Move the Femme & Femme Research Assistant to Anthropic's Managed Platform.pdf

# Playwright CLI scratch output (snapshots/screenshots/logs from manual verification runs)
.playwright-cli/
installer-pkg/dist/
